import { Taskmappingassigntask } from './taskmappingassigntask';

describe('Taskmappingassigntask', () => {
  it('should create an instance', () => {
    expect(new Taskmappingassigntask()).toBeTruthy();
  });
});
