import { TestBed } from '@angular/core/testing';

import { TaskmappingchangecomplianceownerService } from './taskmappingchangecomplianceowner.service';

describe('TaskmappingchangecomplianceownerService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: TaskmappingchangecomplianceownerService = TestBed.get(TaskmappingchangecomplianceownerService);
    expect(service).toBeTruthy();
  });
});
